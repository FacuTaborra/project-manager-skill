# Goes in ~/.claude/pm/credentials.toml — NEVER committed.
# On Linux/macOS: chmod 600 ~/.claude/pm/credentials.toml
#
# `pm creds import` writes this for you from the legacy ~/.claude/secrets/*.env files.

version = 1

[profiles.4plus]
provider     = "clickup"
token        = "pk_xxxxxxxxxxxxx"
workspace_id = "9013377000"        # optional; verified against the token when present

[profiles.interno]
provider     = "clickup"
token        = "pk_yyyyyyyyyyyyy"
workspace_id = "9067890000"

[profiles.personal-linear]
provider = "linear"
token    = "lin_api_xxxxxxxxxxxx"
